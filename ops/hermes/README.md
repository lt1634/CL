# Hermes ops

- **Post-install / 重啟**：[`post-install.sh`](./post-install.sh)（secret 只放 `~/.hermes/.env`）
- **源碼對照**：[`vendor/hermes-agent`](../../vendor/hermes-agent)（submodule，見 `ops/versions.lock.yaml`）
- **運行設定**：`~/.hermes/`（唔 commit）
- **Handoff**：[docs/HANDOFF_HERMES.md](../../docs/HANDOFF_HERMES.md)

舊路徑 `projects/hermes-standalone/` 已改為指向此目錄。
