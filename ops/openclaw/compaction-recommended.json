{
  "_comment": "Merge into ~/.openclaw/openclaw.json — compaction 前 flush durable notes",
  "agents": {
    "defaults": {
      "compaction": {
        "mode": "safeguard",
        "memoryFlush": {
          "enabled": true,
          "softThresholdTokens": 4000,
          "prompt": "Session nearing compaction. Write durable notes to memory/YYYY-MM-DD.md (decisions, todos, user prefs). Update WORKFLOW_AUTO.md current focus (1-3 lines). Do not store secrets or full PII."
        }
      }
    }
  }
}
