# Copy to ~/.openclaw/.env (do not commit secrets)
# Required only when running: node board-writer.mjs serve

BOARD_WRITER_TOKEN=generate-a-long-random-string-here
# Optional overrides:
# COMPANY_BOARD_FILE=$HOME/.openclaw/workspace/company-board.jsonl
# BOARD_WRITER_HOST=127.0.0.1
# BOARD_WRITER_PORT=8765
